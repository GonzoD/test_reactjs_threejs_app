import {makeStyles} from '@material-ui/styles'

const useStyles = makeStyles(() => ({}))

function ListOfCreatedGeometry() {
  const classes = useStyles()

  return (
    <div className={classes.root}>
    </div>
  );
}

export default ListOfCreatedGeometry;